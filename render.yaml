services:
  # 1. The Backend
  - type: web
    name: mern-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGODB_URI
        sync: false 
      - key: NODE_ENV
        value: production

  # 2. The Frontend
  - type: web
    name: mern-frontend
    runtime: static  # This tells Render it's a static site
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routingRules:
      - route: /*
        rewritePath: /index.html